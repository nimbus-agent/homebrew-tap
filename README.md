# Nimbus Homebrew Tap

```sh
brew install nimbus-agent/tap/nimbus
```

Installs the headless Nimbus gateway + CLI. Update with `brew upgrade nimbus`.

`Formula/nimbus.rb` is generated automatically on each stable Nimbus release — do not edit by hand. See https://github.com/nimbus-agent/Nimbus
